let number = [1, 2, 3, 4, 5, 6, 7];
let difference = 'bangladesh is a land of river.';
if (Array.isArray(number)){
  console.log('it is data type is Array');
}
if(Array.isArray(difference) === false){
  console.log('that is not Array data but that is a string data.');
}
